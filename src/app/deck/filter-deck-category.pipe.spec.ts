import { FilterDeckCategoryPipe } from './filter-deck-category.pipe';

describe('FilterDeckCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDeckCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
