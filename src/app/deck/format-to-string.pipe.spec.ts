import { FormatToStringPipe } from './format-to-string.pipe';

describe('FormatToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
